{
  "name": "second_portfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"vite\" \"json-server -p 8080 -w db.json\"",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^4.4.5"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "json-server": "^0.17.4",
    "axios": "^1.6.2"
  }
}
